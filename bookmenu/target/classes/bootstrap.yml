spring:
  application:
    name: bookmenu
  cloud:
    config:
      label: master
      discovery:
        enabled: true
        service-id: CONFIGSERVER
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

